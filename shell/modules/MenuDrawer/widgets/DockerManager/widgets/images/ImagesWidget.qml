import QtQuick
import qs.core

Rectangle {
    anchors.fill: parent
    color: Theme.background

    Text {
        anchors.centerIn: parent
        color: Theme.color8
        font.pixelSize: 18
        text: "💿 Images Management (Coming Soon)"
    }
}
